import * as React from "react";
import { Field } from "formik";
import { Form as AntdForm } from "antd";
import { FormItem } from './FormItem';
export function Form(props) {
    return (React.createElement(Field, null, ({ form: { handleReset, handleSubmit } }) => (React.createElement(AntdForm, Object.assign({ onReset: handleReset, onSubmit: handleSubmit }, props)))));
}
;
Form.Item = FormItem;
//# sourceMappingURL=Form.js.map